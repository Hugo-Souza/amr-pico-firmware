#include "microros_node.h"

bool microros_init(void) {
  // TODO: create node, subscriber /cmd_vel, publishers /odom /status
  return true;
}

void microros_spin_some(void) {
  // TODO: rclc_executor_spin_some with fixed budget
}
